# Schemas

Structured schemas shared across the platform live here.

- `api/` for Pages Functions response contracts.
- `content/` for editorial content shape definitions.
- `ai/` for validated AI output shapes.
- `workflow/` for workflow step payload contracts.

Use these schemas to keep boundaries explicit between Vue, Pages Functions, n8n, and AI steps.